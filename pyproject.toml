[project]
name = "saturday-lotto-lab"
version = "2.0.0"
description = "Mathematically honest Saturday Lotto data analysis and ticket coverage tooling"
requires-python = ">=3.11"
dependencies = ["beautifulsoup4>=4.12,<5"]

[project.scripts]
lotto-lab = "lotto_lab.cli:main"

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py311"
line-length = 110

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
